{
  "name": "apex-playwright-tests",
  "version": "1.0.0",
  "description": "Pruebas automatizadas para APEX CI/CD",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^25.2.3"
  },
  "scripts": {
    "test": "npx playwright test"
  }
}
